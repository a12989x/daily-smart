import React from 'react';

const SearchBar = () => {
    return (
        <form className="searchbar">
            <label>
                Search DailySmarty
                <input type="text" />
            </label>
        </form>
    );
};

export default SearchBar;
